# VM监控系统 - 环境配置模板
# 复制此文件为 .env 并修改配置

# =================== 数据库配置 ===================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_NAME=vm_monitoring
DB_SSLMODE=disable

# =================== Redis配置 ===================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_DB=0

# =================== JWT安全配置 ===================
# ⚠️ 生产环境必须设置此变量
# 生成方式: export JWT_SECRET="$(openssl rand -base64 64)"
JWT_SECRET=

# =================== 应用配置 ===================
APP_MODE=development  # 设置为 production
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# =================== vSphere配置 (可选) ===================
VSPHERE_HOST=vcenter.example.com
VSPHERE_PORT=443
VSPHERE_USERNAME=admin@vsphere.local
VSPHERE_PASSWORD=your-vsphere-password
VSPHERE_INSECURE=false

# =================== 通知配置 (可选) ===================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notifications@example.com
SMTP_PASSWORD=your-smtp-password

# =================== 日志配置 ===================
LOG_LEVEL=info
LOG_FORMAT=json
