# API_AUTH_认证授权模块_API规范

## 文档履历

| 版本 | 日期 | 修改人 | 修改内容 | 审核状态 |
|------|------|--------|----------|----------|
| v1.0 | 2026-02-03 | BE工程师 | 初始版本，基于REQ_20260202和UI_20260202生成 | 🔄 待审核 |

---

## 模块概述

### 功能范围
- 用户身份认证（登录/登出）
- JWT Token管理（签发/刷新/失效）
- 基于角色的访问控制（RBAC）权限校验
- 多语言错误消息支持（en/zh-CN/ja-JP）
- 登录安全策略（锁定、过期、审计日志）

### 适用角色
- 系统管理员、运维工程师、IT经理、安全工程师

---

## 接口清单

| 接口 | 方法 | 路径 | 描述 | 认证要求 |
|------|------|------|------|----------|
| 用户登录 | POST | /api/v1/auth/login | 用户名密码登录，返回Token | 无需认证 |
| 用户登出 | POST | /api/v1/auth/logout | 使当前Token失效 | 需要认证 |
| Token刷新 | POST | /api/v1/auth/refresh | 使用Refresh Token获取新Token | 需要Refresh Token |
| 获取当前用户 | GET | /api/v1/auth/me | 获取当前登录用户信息 | 需要认证 |
| 修改密码 | PUT | /api/v1/auth/password | 修改当前用户密码 | 需要认证 |
| 权限校验 | GET | /api/v1/auth/check | 校验当前用户是否有指定权限 | 需要认证 |

---

## 数据模型

### LoginRequest（登录请求）
```typescript
interface LoginRequest {
  username: string;        // 用户名或邮箱
  password: string;        // 密码（加密传输）
  rememberMe?: boolean;    // 是否记住登录状态（默认false）
  language?: 'en' | 'zh-CN' | 'ja-JP';  // 语言偏好
}
```

### LoginResponse（登录响应）
```typescript
interface LoginResponse {
  user: UserInfo;          // 用户信息
  accessToken: string;     // 访问Token（JWT）
  refreshToken: string;    // 刷新Token
  expiresIn: number;       // Access Token过期时间（秒）
  permissions: string[];   // 用户权限列表
}
```

### UserInfo（用户信息）
```typescript
interface UserInfo {
  id: string;              // 用户ID
  username: string;          // 用户名
  email: string;           // 邮箱
  name: string;            // 显示名称
  department?: string;     // 部门
  roles: RoleInfo[];       // 角色列表
  status: 'active' | 'inactive' | 'locked' | 'expired';  // 账户状态
  lastLoginAt?: Date;      // 最后登录时间
  language: 'en' | 'zh-CN' | 'ja-JP';  // 语言偏好
  mfaEnabled: boolean;     // 是否启用MFA（预留）
}
```

### RoleInfo（角色信息）
```typescript
interface RoleInfo {
  id: string;              // 角色ID
  name: string;            // 角色名称
  description?: string;    // 角色描述
  permissions: string[];   // 权限代码列表
}
```

### TokenPayload（Token载荷）
```typescript
interface TokenPayload {
  sub: string;             // 用户ID
  username: string;        // 用户名
  roles: string[];         // 角色代码列表
  iat: number;           // 签发时间
  exp: number;           // 过期时间
  type: 'access' | 'refresh';  // Token类型
}
```

### PasswordChangeRequest（修改密码请求）
```typescript
interface PasswordChangeRequest {
  oldPassword: string;     // 旧密码
  newPassword: string;     // 新密码
  confirmPassword: string; // 确认新密码
}
```

### PermissionCheckRequest（权限校验请求）
```typescript
interface PermissionCheckRequest {
  permission: string;      // 权限代码
  resource?: string;       // 资源标识（可选）
}
```

---

## 接口详情

### 1. 用户登录

**基本信息**
- 方法: `POST`
- 路径: `/api/v1/auth/login`
- 认证: 无需认证
- Content-Type: `application/json`

**请求参数**
```json
{
  "username": "admin",
  "password": "encrypted_password",
  "rememberMe": true,
  "language": "zh-CN"
}
```

**成功响应 (200)**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "user": {
      "id": "usr_001",
      "username": "admin",
      "email": "admin@company.com",
      "name": "系统管理员",
      "department": "IT部",
      "roles": [
        {
          "id": "role_admin",
          "name": "系统管理员",
          "description": "拥有所有权限",
          "permissions": ["*"]
        }
      ],
      "status": "active",
      "lastLoginAt": "2026-02-03T10:30:00Z",
      "language": "zh-CN",
      "mfaEnabled": false
    },
    "accessToken": "eyJhbGciOiJSUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJSUzI1NiIs...",
    "expiresIn": 3600,
    "permissions": ["vm:read", "vm:write", "alert:read", "alert:write"]
  }
}
```

**错误响应**
- `400` - 请求参数错误（用户名或密码为空）
- `401` - 认证失败（用户名或密码错误）
- `403` - 账户被锁定（需显示锁定时间）
- `403` - 账户已过期
- `429` - 登录尝试次数过多（账户临时锁定）

**安全要求**
1. 密码必须使用RSA公钥加密传输
2. 记录登录日志（IP地址、时间戳、登录结果）
3. 连续5次失败锁定账户30分钟
4. 支持账户过期检测

---

### 2. 用户登出

**基本信息**
- 方法: `POST`
- 路径: `/api/v1/auth/logout`
- 认证: 需要Access Token

**请求头**
```
Authorization: Bearer {access_token}
```

**成功响应 (200)**
```json
{
  "code": 200,
  "message": "登出成功"
}
```

**错误响应**
- `401` - Token无效或已过期

**业务逻辑**
1. 将Token加入黑名单
2. 清除Refresh Token
3. 记录登出日志

---

### 3. Token刷新

**基本信息**
- 方法: `POST`
- 路径: `/api/v1/auth/refresh`
- 认证: 需要Refresh Token

**请求头**
```
Authorization: Bearer {refresh_token}
```

**成功响应 (200)**
```json
{
  "code": 200,
  "message": "刷新成功",
  "data": {
    "accessToken": "eyJhbGciOiJSUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJSUzI1NiIs...",
    "expiresIn": 3600
  }
}
```

**错误响应**
- `401` - Refresh Token无效或已过期

**Token策略**
- Access Token有效期：1小时（3600秒）
- Refresh Token有效期：7天（rememberMe=true时为30天）
- Refresh Token轮转：每次刷新生成新的Refresh Token

---

### 4. 获取当前用户信息

**基本信息**
- 方法: `GET`
- 路径: `/api/v1/auth/me`
- 认证: 需要Access Token

**成功响应 (200)**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": "usr_001",
    "username": "admin",
    "email": "admin@company.com",
    "name": "系统管理员",
    "department": "IT部",
    "roles": [...],
    "status": "active",
    "lastLoginAt": "2026-02-03T10:30:00Z",
    "language": "zh-CN",
    "mfaEnabled": false
  }
}
```

---

### 5. 修改密码

**基本信息**
- 方法: `PUT`
- 路径: `/api/v1/auth/password`
- 认证: 需要Access Token

**请求参数**
```json
{
  "oldPassword": "encrypted_old_password",
  "newPassword": "encrypted_new_password",
  "confirmPassword": "encrypted_new_password"
}
```

**成功响应 (200)**
```json
{
  "code": 200,
  "message": "密码修改成功",
  "data": {
    "passwordChangedAt": "2026-02-03T11:00:00Z"
  }
}
```

**错误响应**
- `400` - 新密码不符合复杂度要求
- `400` - 两次输入的新密码不一致
- `401` - 旧密码错误

**密码复杂度要求**
- 最少8位字符
- 必须包含大小写字母
- 必须包含数字
- 必须包含特殊字符（!@#$%^&*）

---

### 6. 权限校验

**基本信息**
- 方法: `GET`
- 路径: `/api/v1/auth/check`
- 认证: 需要Access Token

**查询参数**
```
GET /api/v1/auth/check?permission=vm:write&resource=vm_001
```

**成功响应 (200)**
```json
{
  "code": 200,
  "message": "校验完成",
  "data": {
    "allowed": true,
    "permission": "vm:write",
    "resource": "vm_001"
  }
}
```

**权限代码格式**
- 格式：`{resource}:{action}`
- 示例：
  - `vm:read` - 查看VM信息
  - `vm:write` - 编辑VM信息
  - `alert:read` - 查看告警
  - `alert:write` - 管理告警规则
  - `user:read` - 查看用户信息
  - `user:write` - 管理用户
  - `system:admin` - 系统管理员权限（通配）

---

## 错误码定义

| 错误码 | 英文消息 | 中文消息 | 日文消息 | 说明 |
|--------|---------|---------|---------|------|
| 400 | Bad Request | 请求参数错误 | リクエストパラメータエラー | 参数缺失或格式错误 |
| 401 | Unauthorized | 未授权，请重新登录 | 未認証、再ログインしてください | Token无效或过期 |
| 403 | Forbidden | 权限不足 | アクセス権限がありません | 无权限访问资源 |
| 403-LOCKED | Account Locked | 账户已锁定，请{minutes}分钟后重试 | アカウントがロックされました、{minutes}分後に再試行してください | 账户锁定 |
| 403-EXPIRED | Account Expired | 账户已过期，请联系管理员 | アカウントの有効期限が切れました、管理者に連絡してください | 账户过期 |
| 401-INVALID | Invalid Credentials | 用户名或密码错误 | ユーザー名またはパスワードが間違っています | 认证失败 |
| 429 | Too Many Requests | 登录尝试次数过多，请稍后再试 | ログイン試行回数が多すぎます、後でもう一度お試しください | 频率限制 |
| 400-PASSWORD | Invalid Password Format | 密码复杂度不符合要求 | パスワードの複雑さが要件を満たしていません | 密码格式错误 |
| 400-MISMATCH | Password Mismatch | 两次输入的密码不一致 | 入力されたパスワードが一致しません | 密码不匹配 |

---

## 安全规范

### 1. 传输安全
- 所有接口强制HTTPS
- 密码必须使用RSA公钥加密后传输
- Token在Header中传输：`Authorization: Bearer {token}`

### 2. Token安全
- Access Token有效期1小时
- Refresh Token实现轮转机制
- Token黑名单机制支持主动失效
- JWT使用RS256算法签名

### 3. 登录安全
- 连续5次失败锁定账户30分钟
- 登录日志记录（IP、时间、结果）
- 异地登录检测（预留）
- 支持强制修改密码策略

### 4. 审计日志
- 记录所有认证相关操作
- 日志包含：用户ID、操作类型、时间、IP地址、结果
- 日志保留180天

---

## 多语言支持

### 语言代码
- `en` - English（默认）
- `zh-CN` - 简体中文
- `ja-JP` - 日本語

### 消息优先级
1. 请求中指定的`language`参数
2. 用户个人设置中的语言偏好
3. 请求头`Accept-Language`
4. 默认语言（en）

---

## 变更记录

### 版本 v1.0 (2026-02-03)
**修改人**: BE工程师  
**修改原因**: 基于REQ_20260202_VM监控系统需求文档初始创建  
**具体修改**:
- [x] 新增登录/登出接口
- [x] 新增Token管理接口
- [x] 新增用户信息查询接口
- [x] 新增权限校验接口
- [x] 定义数据模型（UserInfo, RoleInfo, TokenPayload等）
- [x] 定义错误码（支持多语言）
- [x] 定义安全规范

**影响范围**:
- 前端界面: 是（登录页面、用户菜单）
- 后端API: 是（认证中间件、权限控制）
- 数据库结构: 是（users, roles, permissions表）
- 部署配置: 是（JWT密钥配置、HTTPS强制）

**相关文档**:
- REQ_20260202_VM监控系统.md（用户角色、安全性要求章节）
- UI_20260202_VM监控系统_视觉设计指南.md（登录页面设计）

---

**文档管理说明**:
1. 此文档为BE/FE契约文件，任何变更需同步更新
2. 字段变更需记录在`api-changes.md`
3. 版本号遵循语义化版本规范
4. 所有修改需通知相关干系人
