# 环境搭建指南

## 文档履历

| 版本 | 日期 | 修改人 | 修改内容 | 审核状态 |
|------|------|--------|----------|----------|
| v1.0 | 2026-02-02 | 基础设施架构师 | 初始版本，完整的Windows + WSL2环境搭建指南 | ✅ 已完成 |

---

## 📋 目录

1. [环境要求](#环境要求)
2. [Windows环境准备](#windows环境准备)
3. [WSL2安装配置](#wsl2安装配置)
4. [Docker Desktop配置](#docker-desktop配置)
5. [Linux开发环境配置](#linux开发环境配置)
6. [项目结构创建](#项目结构创建)
7. [开发服务启动](#开发服务启动)
8. [开发工作流](#开发工作流)
9. [常见问题解决](#常见问题解决)
10. [性能优化建议](#性能优化建议)

---

## 环境要求

### 硬件要求

| 组件 | 最低配置 | 推荐配置 | 当前配置 |
|------|----------|----------|----------|
| CPU | 4核心 | 8核心+ | 6核心 ✅ |
| 内存 | 8GB | 16GB+ | 16GB ✅ |
| 存储 | 100GB SSD | 200GB+ NVMe SSD | - |
| 网络 | 100Mbps | 1Gbps | - |

### 软件要求

| 软件 | 最低版本 | 推荐版本 | 安装方式 |
|------|----------|----------|----------|
| Windows | 10 Pro | 11 Pro | - |
| WSL | WSL2 | WSL2 | Microsoft Store |
| Docker Desktop | 4.15+ | 最新版 | 官方安装包 |
| VS Code | 1.70+ | 最新版 | 官方安装包 |
| Git | 2.30+ | 最新版 | 官方安装包 |

---

## Windows环境准备

### 1. 启用WSL功能

以管理员身份打开PowerShell，执行以下命令：

```powershell
# 启用WSL
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart

# 启用虚拟机平台
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# 下载WSL2 Linux内核更新
Invoke-WebRequest -Uri https://wslstorestorage.blob.core.windows.net/wslblob/wsl_update_x64.msi -OutFile wsl_update_x64.msi

# 安装WSL2更新
Start-Process msiexec.exe -Wait -ArgumentList '/I wsl_update_x64.msi /quiet'

# 设置WSL2为默认版本
wsl --set-default-version 2

# 重启计算机
Restart-Computer
```

### 2. 安装Ubuntu 22.04 LTS

打开Microsoft Store，搜索并安装"Ubuntu 22.04.3 LTS"，或使用PowerShell：

```powershell
# 使用WSL命令直接安装
wsl --install -d Ubuntu-22.04
```

安装完成后，启动Ubuntu并创建用户账户。

### 3. 安装Windows开发工具

#### Git安装
```powershell
# 下载Git安装程序
Invoke-WebRequest -Uri https://github.com/git-for-windows/git/releases/download/v2.43.0.windows.1/Git-2.43.0-64-bit.exe -OutFile git-installer.exe

# 运行安装程序
Start-Process git-installer.exe
```

#### VS Code安装
```powershell
# 下载VS Code安装程序
Invoke-WebRequest -Uri https://code.visualstudio.com/sha/download?build=stable&os=win32-x64-user -OutFile vscode-installer.exe

# 运行安装程序
Start-Process vscode-installer.exe
```

#### 安装VS Code扩展
在VS Code中安装以下扩展：
- Remote - WSL (ms-vscode-remote.remote-wsl)
- Docker (ms-azuretools.vscode-docker)
- GitLens (eamodio.gitlens)
- ESLint (dbaeumer.vscode-eslint)
- Prettier (esbenp.prettier-vscode)

---

## WSL2安装配置

### 1. WSL2性能优化配置

在Windows用户目录下创建`.wslconfig`文件：

```powershell
# 创建WSL2配置文件
$wslConfig = @"
[wsl2]
# 内存分配（10GB给WSL2）
memory=10GB

# 处理器分配（5核心给WSL2）
processors=5

# 交换空间（4GB）
swap=4GB

# 交换文件路径
swapFile=C:\\temp\\wsl-swap.vhdx

# 启用本地主机转发
localhostForwarding=true

# 启用嵌套虚拟化
nestedVirtualization=true

# 配置WSL2内核参数
kernelCommandLine = net.core.default_qdisc=fq
"@

# 写入配置文件
$wslConfig | Out-File -FilePath "$env:USERPROFILE\.wslconfig" -Encoding UTF8

echo "WSL2配置文件已创建"
echo "重启WSL2以应用配置"
```

### 2. 重启WSL2

```powershell
# 关闭所有WSL2实例
wsl --shutdown

# 重新启动WSL2
wsl -d Ubuntu-22.04
```

### 3. 验证WSL2配置

在WSL2中运行：

```bash
# 检查WSL版本
cat /proc/version
# 应该看到 "microsoft-standard-WSL2"

# 检查内存
free -h

# 检查CPU
nproc
```

---

## Docker Desktop配置

### 1. 安装Docker Desktop

下载最新版本的Docker Desktop：
- 官方下载地址: https://www.docker.com/products/docker-desktop
- 选择: Docker Desktop for Windows

### 2. Docker Desktop设置

打开Docker Desktop，进行以下配置：

#### General设置
```
✅ Use the WSL 2 based engine
✅ Start Docker Desktop when you log in
✅ Expose daemon on tcp://localhost:2375 without TLS
```

#### Resources > WSL Integration
```
✅ Enable integration with my default WSL distro
✅ Ubuntu-22.04 (选择Ubuntu)
```

#### Resources > Advanced
```
内存: 4GB
交换: 2GB
磁盘映像大小: 100GB
```

#### Resources > File sharing
```
✅ 保留默认设置，不要启用Windows文件共享
```

### 3. 验证Docker安装

在WSL2中运行：

```bash
# 验证Docker安装
docker --version

# 验证Docker Compose
docker-compose --version

# 测试Docker运行
docker run --rm hello-world

# 应该看到 "Hello from Docker!" 消息
```

---

## Linux开发环境配置

### 1. 更新系统

```bash
# 更新软件包列表
sudo apt update

# 升级已安装的软件包
sudo apt upgrade -y

# 安装必要的基础工具
sudo apt install -y curl wget git vim htop net-tools build-essential software-properties-common apt-transport-https ca-certificates gnupg lsb-release
```

### 2. 安装Node.js 18+

```bash
# 添加NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# 安装Node.js
sudo apt install -y nodejs

# 验证安装
node --version   # 应该是 v18.x
npm --version    # 应该是 9.x 或更高
```

### 3. 安装pnpm

```bash
# 全局安装pnpm
npm install -g pnpm

# 验证安装
pnpm --version
```

### 4. 安装其他开发工具

```bash
# 安装Python 3
sudo apt install -y python3 python3-pip python3-venv

# 安装Java (如果需要）
sudo apt install -y openjdk-17-jdk

# 安装其他实用工具
sudo apt install -y jq tree unzip zip
```

### 5. 配置Git

```bash
# 配置Git用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 配置Git编辑器
git config --global core.editor "vim"

# 配置Git凭证存储
git config --global credential.helper "store"

# 生成SSH密钥（如果需要）
ssh-keygen -t rsa -b 4096 -C "your.email@example.com"

# 显示公钥（用于GitHub/GitLab等）
cat ~/.ssh/id_rsa.pub
```

### 6. 安装开发数据库

#### PostgreSQL安装
```bash
# 安装PostgreSQL
sudo apt install -y postgresql postgresql-contrib

# 启动PostgreSQL服务
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 设置PostgreSQL密码
sudo -u postgres psql
# 在psql中执行:
ALTER USER postgres WITH PASSWORD 'your_password';
\q
```

#### Redis安装
```bash
# 安装Redis
sudo apt install -y redis-server

# 启动Redis服务
sudo systemctl start redis-server
sudo systemctl enable redis-server

# 验证Redis
redis-cli ping
# 应该返回 PONG
```

---

## 项目结构创建

### 1. 创建项目根目录

```bash
# 在WSL2文件系统中创建项目目录（重要！）
cd ~
mkdir -p projects/vm-monitor
cd projects/vm-monitor

# 创建项目子目录
mkdir -p {backend,frontend,scripts,docker,docs,tests}
```

### 2. 初始化项目

```bash
# 初始化Git仓库
git init

# 创建.gitignore文件
cat > .gitignore << 'EOF'
# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore

# Temporary files
*.tmp
*.swp
*~
EOF

# 验证.gitignore
cat .gitignore
```

### 3. 创建Docker Compose配置

从之前的配置文件复制优化版Docker Compose配置：

```bash
# 复制Docker Compose配置
cp /path/to/docker-compose.optimized.yml ./docker-compose.yml
```

---

## 开发服务启动

### 1. 使用服务管理脚本

```bash
# 赋予脚本执行权限
chmod +x scripts/manage-services.sh

# 启动基础开发环境
./scripts/manage-services.sh basic

# 查看服务状态
./scripts/manage-services.sh status

# 查看资源使用情况
./scripts/manage-services.sh resources
```

### 2. 手动启动Docker Compose

```bash
# 启动所有服务
docker-compose up -d

# 启动特定服务
docker-compose up -d postgresql redis

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 3. 验证服务连接

```bash
# 测试PostgreSQL
docker exec -it vm-monitor-postgres psql -U admin -d vm_monitor -c "SELECT version();"

# 测试Redis
docker exec -it vm-monitor-redis redis-cli ping

# 测试InfluxDB
curl -s "http://localhost:8086/ping"
```

---

## 开发工作流

### 1. 日常开发流程

```bash
# 步骤1: 启动开发环境
cd ~/projects/vm-monitor
./scripts/manage-services.sh basic

# 步骤2: 启动后端服务
cd backend
pnpm install
pnpm run dev

# 步骤3: 启动前端服务（新终端）
cd ~/projects/vm-monitor/frontend
pnpm install
pnpm run dev

# 步骤4: 在Windows浏览器中访问
# 前端: http://localhost:3000
# 后端: http://localhost:3001
```

### 2. 数据库连接配置

```javascript
// backend/.env.example
DATABASE_URL=postgresql://admin:password@localhost:5432/vm_monitor
REDIS_URL=redis://localhost:6379
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=admin_token
INFLUXDB_ORG=vm-monitor
INFLUXDB_BUCKET=metrics
RABBITMQ_URL=amqp://admin:password@localhost:5672
```

### 3. VS Code Remote-WSL开发

在Windows中打开VS Code：

1. 按 `Ctrl+Shift+P` 打开命令面板
2. 输入 "WSL: Connect to WSL"
3. 选择 "Ubuntu-22.04"
4. 打开项目目录: `File -> Open Folder` -> `~/projects/vm-monitor`

### 4. 测试流程

```bash
# 运行单元测试
cd backend
pnpm test

cd ../frontend
pnpm test

# 运行集成测试
pnpm test:integration

# 运行E2E测试
pnpm test:e2e
```

---

## 常见问题解决

### 问题1: WSL2内存不足

**症状**: Docker容器经常重启，系统变慢

**解决方案**:
```powershell
# 1. 增加WSL2内存分配
# 编辑 C:\Users\<YourUsername>\.wslconfig
# 将 memory=10GB 改为 memory=12GB

# 2. 重启WSL2
wsl --shutdown
wsl -d Ubuntu-22.04

# 3. 减少Docker资源占用
# 在Docker Desktop中减少Docker内存分配到4GB或更低
```

### 问题2: 文件性能差

**症状**: 在WSL2中访问Windows文件系统(/mnt/c/)很慢

**解决方案**:
```bash
# ✅ 推荐: 在WSL2文件系统中工作
cd ~/projects/vm-monitor

# ❌ 避免: 在Windows挂载点中工作
cd /mnt/c/Users/YourName/projects/vm-monitor

# 如果必须访问Windows文件，使用WSL2缓存
# 编辑 /etc/wsl.conf
sudo vim /etc/wsl.conf

# 添加以下内容:
[automount]
options = "metadata"
enabled = true
```

### 问题3: Docker容器无法访问网络

**症状**: Docker容器无法访问外部网络或服务

**解决方案**:
```bash
# 1. 重启Docker Desktop
# 2. 检查WSL2网络配置
cat /etc/resolv.conf

# 3. 清理Docker网络
docker network prune

# 4. 重启Docker服务
sudo systemctl restart docker
```

### 问题4: 端口冲突

**症状**: 服务无法启动，提示端口已被占用

**解决方案**:
```bash
# 查看端口占用情况
sudo netstat -tulpn | grep :5432
sudo netstat -tulpn | grep :6379

# 停止占用端口的进程
sudo kill -9 <PID>

# 或修改Docker Compose配置中的端口映射
# 将 "5432:5432" 改为 "5433:5432"
```

### 问题5: Node.js版本问题

**症状**: Node.js命令找不到或版本不匹配

**解决方案**:
```bash
# 1. 验证Node.js安装
which node
node --version

# 2. 重新安装Node.js
sudo apt remove --purge nodejs npm
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 3. 验证安装
node --version
npm --version
```

### 问题6: Docker权限问题

**症状**: 需要sudo才能运行docker命令

**解决方案**:
```bash
# 1. 将用户添加到docker组
sudo usermod -aG docker $USER

# 2. 重新登录或运行
newgrp docker

# 3. 验证权限
docker run --rm hello-world
```

---

## 性能优化建议

### 1. WSL2性能优化

```bash
# 在Windows上创建性能配置文件
# C:\Users\<YourUsername>\.wslconfig

[wsl2]
# 优化内存使用
memory=10GB
processors=5

# 优化交换
swap=4GB
swapFile=C:\\temp\\wsl-swap.vhdx

# 启用嵌套虚拟化
nestedVirtualization=true

# 优化网络
localhostForwarding=true

# 优化内核
kernelCommandLine = net.core.default_qdisc=fq
```

### 2. Docker性能优化

```yaml
# docker-compose.optimized.yml

services:
  postgresql:
    # 资源限制
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
    
    # 性能优化
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=1GB"
```

### 3. 开发流程优化

```bash
# 1. 使用项目特定的shell别名
echo 'alias vm-monitor="cd ~/projects/vm-monitor"' >> ~/.bashrc
echo 'alias vm-start="cd ~/projects/vm-monitor && ./scripts/manage-services.sh basic"' >> ~/.bashrc
echo 'alias vm-stop="cd ~/projects/vm-monitor && ./scripts/manage-services.sh stop"' >> ~/.bashrc

# 2. 重新加载配置
source ~/.bashrc

# 3. 使用快捷命令
vm-start    # 启动开发环境
vm-monitor  # 进入项目目录
vm-stop     # 停止所有服务
```

### 4. VS Code性能优化

```json
// .vscode/settings.json

{
  // 文件监视优化
  "files.watcherExclude": {
    "**/.git/objects/**": true,
    "**/.git/subtree-cache/**": true,
    "**/node_modules/**": true,
    "**/.hg/store/**": true
  },
  
  // 终端优化
  "terminal.integrated.defaultProfile.linux": "bash",
  
  // 编辑器优化
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  
  // TypeScript优化
  "typescript.tsserver.maxTsServerMemory": 4096
}
```

---

## 快速开始检查清单

- [ ] Windows 10/11 Pro 已安装
- [ ] WSL2 已启用并安装 Ubuntu 22.04
- [ ] WSL2 性能配置已设置
- [ ] Docker Desktop 已安装并配置
- [ ] Node.js 18+ 已安装
- [ ] pnpm 已安装
- [ ] Git 已配置
- [ ] VS Code 和 Remote-WSL 扩展已安装
- [ ] 项目目录结构已创建
- [ ] Docker Compose 配置已设置
- [ ] 基础服务已启动并验证
- [ ] 数据库连接已配置
- [ ] 前端和后端服务已启动
- [ ] 浏览器可以访问应用

---

## 获取帮助

### 常用命令速查

```bash
# WSL2管理
wsl --shutdown                          # 关闭WSL2
wsl -d Ubuntu-22.04                    # 启动特定发行版
wsl --list -v                          # 列出所有WSL发行版

# Docker管理
docker ps                              # 列出运行的容器
docker logs <container>                 # 查看容器日志
docker-compose up -d                    # 启动服务
docker-compose down                      # 停止服务

# 资源监控
htop                                  # 系统资源监控
docker stats                           # Docker资源监控
free -h                                # 内存使用情况
df -h                                  # 磁盘使用情况

# 服务管理
./scripts/manage-services.sh basic      # 启动基础环境
./scripts/manage-services.sh backend     # 启动后端环境
./scripts/manage-services.sh status      # 查看服务状态
./scripts/manage-services.sh resources   # 查看资源使用
```

### 联系方式

如遇到问题，可以：
1. 查看本文档的"常见问题解决"章节
2. 查看项目GitHub Issues
3. 联系技术支持团队

---

**文档版本**: v1.0  
**最后更新**: 2026-02-02  
**适用环境**: Windows 10/11 + WSL2 + Docker Desktop  
**目标配置**: 16GB内存 + 6核心CPU